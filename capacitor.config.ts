
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fi.raamattuopisto.srotech',
  appName: 'SRO Tech',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
